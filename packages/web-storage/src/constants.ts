/**
 * Default values used to fill missing configuration options
 * @module WebStorageConstants
 * */

/** default pattern visited spots cookie prefix */
export const DEFAULT_COOKIE_PREFIX = 'geoxp-pattern-';

/** pattern visited spots cookie default expiration time [minutes] */
export const DEFAULT_COOKIE_EXPIRATION = 5;

/** self clean when last spot is activated */
export const DEFAULT_DELETE_ON_LAST_SPOT = false;

/** self clean when all patterns spots have been visisted */
export const DEFAULT_DELETE_ON_COMPLETION = false;
