/**
 * Default values used to fill missing configuration options
 * @module Constants
 * */

/** default fade in time [milliseconds] */
export const DEFAULT_FADE_IN_TIME = 0;

/** default fade out time [milliseconds] */
export const DEFAULT_FADE_OUT_TIME = 1000;
