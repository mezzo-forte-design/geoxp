{
  "name": "@geoxp/web-audio",
  "version": "1.0.0",
  "description": "Mezzoforte GeoXp Web Audio module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build -w core && npm run typecheck && webpack --mode production --env=production",
    "build:dev": "npm run build -w core && webpack --mode development --env=development",
    "build:ext": "npm run typecheck:ext && webpack --mode production --env=production",
    "build:ext:dev": "webpack --mode development --env=development",
    "build:clear": "rm -rf dist",
    "typecheck": "npm run build -w core && tsc --noEmit",
    "typecheck:ext": "tsc --noEmit"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "core": "^1.0.0",
    "howler": "^2.2.4"
  },
  "devDependencies": {
    "@types/howler": "^2.2.11"
  }
}
